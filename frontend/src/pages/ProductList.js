import React from 'react';
import Sidebar from '../components/Sidebar';

const ProductList = () => {
    return (
        <div>
           <Sidebar/>
        </div>
    );
};

export default ProductList;